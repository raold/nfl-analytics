$pdf_mode = 1;
# Force all outputs (PDF and aux) into the current directory
$out_dir = '.';
$aux_dir = '.';

# Be explicit about the default job name
$jobname = 'main';

# Quiet unless there are errors
$silent = 0;
